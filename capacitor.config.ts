import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.camera.issue',
  appName: 'cs-camera-issue',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
